{
  "name": "cron-self-code",
  "version": "1.0.0",
  "description": "Hourly cron job that calls API based on hour divisibility",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node index.js",
    "deploy": "dotenv -e .env -- bash deploy.sh"
  },
  "keywords": ["cron", "api", "scheduler"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-cron": "^3.0.3",
    "axios": "^1.6.2"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "dotenv-cli": "^7.3.0"
  }
}
